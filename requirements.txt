# Core dependencies
numpy>=1.26.0
pandas>=2.0.0
pillow>=10.0.0
tqdm>=4.65.0

# PyTorch (install separately based on your CUDA version)
# See: https://pytorch.org/get-started/locally/
torch>=2.0.0
torchvision>=0.15.0

# Image processing
scikit-image>=0.21.0
opencv-python>=4.8.0

# Visualization
matplotlib>=3.7.0

# HuggingFace for model loading
huggingface_hub>=0.20.0

# CVPR video training & eval (install from repo root: pip install -e ".[train]")
hydra-core>=1.3.0
submitit>=1.5.0
tensorboard>=2.14.0
ftfy==6.1.1
regex
iopath>=0.1.10
timm>=1.0.17
torchmetrics>=1.0.0
fvcore
fairscale
decord
